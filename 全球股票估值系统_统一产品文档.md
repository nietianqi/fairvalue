# 全球股票估值系统_统一产品文档

更新日期：2026-04-03  
适用对象：产品、后端、数据工程、前端、Codex、Claude Code  
文档定位：全局单一产品基线（Global Product Baseline）

## 1. 文档来源与主次关系

当前以以下文档为准：

1. [全球股票估值系统_统一产品文档.md](F:/fairvalue/全球股票估值系统_统一产品文档.md)
2. [美国股票估值.md](F:/fairvalue/美国股票估值.md)
3. [美股估值系统_完整终版方案_v2.docx](F:/fairvalue/美股估值系统_完整终版方案_v2.docx)
4. [中国股票估值引擎_PRD_正式版.docx](F:/fairvalue/中国股票估值引擎_PRD_正式版.docx)
5. [us_equity_valuation_complete_final_plan.md](F:/fairvalue/us_equity_valuation_complete_final_plan.md)
6. [us_equity_valuation_api_openapi_spec.md](F:/fairvalue/us_equity_valuation_api_openapi_spec.md)

说明：

1. 全球产品范围、跨市场接口和实现状态，以本文件为准。
2. 美国市场深度规则以 [美国股票估值.md](F:/fairvalue/美国股票估值.md) 为准。
3. 美国终版结构和方法优先级，以 [美股估值系统_完整终版方案_v2.docx](F:/fairvalue/美股估值系统_完整终版方案_v2.docx) 为准。
4. 中国市场规则以 [中国股票估值引擎_PRD_正式版.docx](F:/fairvalue/中国股票估值引擎_PRD_正式版.docx) 和配套规则文件为准。
5. 旧的全球 PRD、重复功能清单和旧商业化文档不再作为主文档使用。

## 2. 产品定义

这不是一个只返回单点目标价的小工具，而是一个覆盖美国、中国、日本、香港四个市场的：

**可追溯、可解释、可 API 化、可商业化的股票估值能力平台。**

系统输出的核心对象不是单一 target price，而是：

1. `fair_value_low / fair_value_mid / fair_value_high`
2. `blended_intrinsic_value`
3. `margin_of_safety`
4. `verdict`
5. `scenario_matrix`
6. `risk_matrix`
7. `implied_expectation`
8. `data_quality_audit`
9. `financial_quality_scorecard`
10. `source_attribution`
11. `history`
12. `rankings / peers / screener`

## 3. 当前代码架构梳理

### 3.1 后端主结构

1. `com.fairvalue.engine.api`
   - 通用估值接口、CN/JP/US 专属接口、后台管理接口
2. `com.fairvalue.engine.service`
   - 通用估值服务、市场发现、市场数据读取、历史与筛选逻辑
3. `com.fairvalue.engine.us`
   - US 数据接入、标准化、质量评分、风险矩阵、方法组合、估值持久化、读库服务、定时刷新
4. `com.fairvalue.engine.cn`
   - CN 规则引擎和 Eastmoney live 接入
5. `com.fairvalue.engine.jp`
   - JP 专属接口与规则化估值服务
6. `com.fairvalue.engine.repository`
   - PostgreSQL / JdbcClient 仓储层
7. `src/main/resources/db/migration`
   - Flyway 迁移，当前已到 `V10`
8. `src/main/resources/static`
   - 全球榜单页、US 详情页、US 后台管理页

### 3.2 当前 US 估值计算与读取链路

当前美股已明确拆成两条链：

1. 计算链
   - `POST /v1/us-equities/{ticker}/valuation/run`
   - 计算适用方法
   - 写入 `valuation_runs` 及子表
   - 同时 `upsert valuation_latest_snapshot`
   - 定时调度写入 `valuation_jobs`
2. 读取链
   - `GET summary / report / rankings` 优先读取数据库
   - `history` 优先读取 `market_price_daily + valuation_runs`
   - `peers` 优先复用最近 run 和 latest snapshot
   - `screener` 优先读取 latest snapshot，不再对每只股票触发重型 live 估值

### 3.3 当前 US 定时刷新模型

1. 已接入 `@EnableScheduling`
2. 已新增 `UsScheduledValuationRefreshService`
3. 默认每 30 分钟刷新一次 `US security_master` 全量 universe
4. 定时任务写库模式：`run_mode = scheduled`
5. 手动重算写库模式：`run_mode = manual`
6. 当前已具备 `valuation_jobs` 基础状态流转与失败重试窗口
7. 当前已具备 `valuation_jobs` claim / retry / recover / alerts 基础能力
8. 当前还没有把 `valuation_jobs` 做成完整 worker / 优先级 / 告警体系

### 3.4 当前 US 全量 universe 状态

1. `US discovery` 已切到 `security_master` 全量分页读取，不再只依赖内存 seed universe
2. `US rankings` 已切到 `security_master` 全量分页口径
3. 当前 live 数据库在最近一次 `SEC universe sync` 后，活跃 `US` 证券数已达到 `8072`
4. 当前 `rankings` 仍处于过渡态：
   - 分页范围已经是全量 `US security_master`
   - 但在 `valuation_latest_snapshot` 还未覆盖全量 universe 之前，排序仍是 `page-scoped`
5. 当前产品口径应解释为：
   - `discovery = full-universe paged`
   - `rankings = full-universe paged, page-scoped ordering until snapshot coverage is complete`

## 4. 数据源策略

### 4.1 当前正式 / 半正式数据源矩阵

| 市场 | 当前主链路 | 状态 |
| --- | --- | --- |
| 美国 | SEC EDGAR + 公司 IR + Longbridge + FRED + Damodaran + SimFin(部分) | 最完整 |
| 中国 | Eastmoney + CN 规则引擎 | 可运行但未完全数据库化 |
| 日本 | 本地规则 + 通用快照 | 基础版 |
| 香港 | 通用快照 + 通用估值 | 基础版 |

### 4.2 源类型说明

1. `SEC EDGAR`
   - 用于 filings、companyfacts、shares、revenue、cash flow、debt、equity 等主事实
2. `公司 IR`
   - 用于 guidance、presentation、管理层补充说明
3. `Longbridge`
   - 用于 US 价格和市场快照主源
4. `FRED`
   - 用于 risk free rate / terminal growth 等宏观参数增强
5. `Damodaran`
   - 用于 ERP / beta / 行业倍数参考
6. `SimFin`
   - 当前已接 `companies` 和状态探测，`derived` 受订阅限制
7. `Eastmoney`
   - 当前为 CN quote/discovery 主源

## 5. 当前已实现的功能

### 5.1 全局已实现

1. 通用估值接口：`GET /v1/valuation/{market}/{symbol}`
2. discovery：`GET /v1/discovery/{market}`
3. rankings：`GET /v1/rankings/{market}/undervalued`、`GET /v1/rankings/{market}/overvalued`
4. peers：`GET /v1/peers/{market}/{symbol}`
5. batch：`POST /v1/valuation/batch`
6. explain：`GET /v1/valuation/{market}/{symbol}/explain`
7. scenario：`POST /v1/valuation/scenario`
8. history：`GET /v1/valuation/history/{market}/{symbol}`
9. screener：`POST /v1/screener/valuation`
10. 多市场统一 `ValuationService`

### 5.2 美国市场已实现

1. `security_master / security_identifier_map`
2. `source_documents / source_document_facts_raw`
3. SEC universe、submissions、companyfacts 同步
4. 公司 IR 元数据同步
5. `financial_standardized`
6. `financial_derived_metrics`
7. `financial_quality_scores`
8. `data_quality_audit`
9. `market_data_raw / market_price_daily / market_snapshot / market_intraday_snapshot`
10. `Longbridge / FRED / Damodaran / SimFin` 客户端与 `source-status`
11. 美国专属接口：
   - `/v1/us-equities/{ticker}/profile`
   - `/v1/us-equities/{ticker}/data-quality`
   - `/v1/us-equities/{ticker}/financial-quality`
   - `/v1/us-equities/{ticker}/valuation/run`
   - `/v1/us-equities/{ticker}/valuation/summary`
   - `/v1/us-equities/{ticker}/valuation/report`
12. 美国后台接口：
   - `/v1/us-equities-admin/{ticker}/overview`
   - `/v1/us-equities-admin/{ticker}/source-status`
   - `universe / sec / ir / market / standardize sync`
13. 4 个核心估值方法已落库：
   - `dcf`
   - `historical_multiple`
   - `relative_valuation`
   - `reverse_dcf`
14. `summary / decision / explanation` 三层输出已存在
15. explanation 固定 `12` 个 blocks
16. risk matrix 已进入估值修正链路
17. source attribution v2 已进入主链路
18. `US peers GET` 已改为只读，不再写新的 `valuation_runs`
19. `valuation_latest_snapshot` 已落地
20. `summary / report / rankings` 已支持优先读库
21. `history` 已优先读真实持久化历史
22. `screener` 已优先读 latest snapshot，缺失时走轻量估值
23. 已接 30 分钟 `US security_master` universe 定时刷新骨架
24. `valuation_jobs` 已有后台统计与 ticker 维度查询
25. `valuation_jobs` 已支持后台失败重试与卡死恢复：
   - `POST /v1/us-equities-admin/valuation-jobs/retry-failed`
   - `POST /v1/us-equities-admin/{ticker}/valuation-jobs/retry-failed`
   - `POST /v1/us-equities-admin/valuation-jobs/recover-stale`
26. `valuation_jobs` 已进入队列化基础阶段：
   - 已持久化 `priority / available_at / worker_id`
   - scheduler 会先 `claim queued jobs` 再执行
   - stale running jobs 现在按“重新排队恢复”处理，而不是直接终止
27. `valuation-alerts` 已可用：
   - `GET /v1/us-equities-admin/valuation-alerts`
   - 已返回 `queued / running / backlog / stale / recent_failed`
28. `US peers` 当前已具备行业规则只读 peer universe 与 `snapshot_fallback`，不再默认空集合
29. `US peers snapshot_fallback` 已按以下维度排序与分组：
   - `industry`
   - `sector_template`
   - `company_type`
   - `market_cap / pe / revenue_growth / fcf_margin / roic / liquidity`
30. 平台层已正式进入主运行态：
   - 统一 envelope：`success / request_id / timestamp / data`
   - API key 鉴权
   - rate limit
   - entitlement 检查
   - usage metering
   - `/platform/bootstrap`
   - `/v1/platform/me`
   - `/v1/platform/usage`
31. `US peers` 的 `verdict` 已统一收口为稳定枚举：
   - `UNDERVALUED`
   - `OVERVALUED`
   - `FAIR`

### 5.3 中国市场已实现

1. 中国专属 controller
2. `valuation/run`、`summary`、`report`
3. `discovery`
4. `rules/version`
5. Eastmoney live quote
6. Eastmoney universe discovery
7. 中国规则文件和行业路由逻辑

### 5.4 日本市场已实现

1. 日本专属 controller
2. `overview`
3. `fair-value`
4. `history`
5. `screener`
6. `recalc`
7. `events`

### 5.5 前端与后台页面已实现

1. 全球榜单页基础版
2. 美国详情页基础版
3. 美国后台管理页
4. 全球筛选页基础版
5. 市场切换、榜单渲染、详情 tab、风险/方法/历史展示
6. 美国后台页已支持：
   - source status
   - valuation alerts
   - platform usage snapshot
   - valuation jobs summary
   - ticker recent jobs
   - global recent jobs
   - retry failed jobs
   - recover stale jobs

## 6. 当前未实现或未完成的功能

### 6.1 全局未完成

1. 统一 API envelope 已进入主运行态，但还没有全平台单一契约收口
2. API key 鉴权已进入主运行态，但还没有正式 key 管理、用户绑定和后台控制台
3. 限流、entitlement、usage metering 已进入主运行态，但还没有套餐、配额、超额与 client lifecycle 管理
4. 多租户 / 白标能力
5. 用户系统、付费系统、账单系统
6. watchlist / alert / webhook 正式产品链路
7. 真正的全市场持久化 ranking engine

### 6.2 美国市场未完成

1. `valuation_jobs` 已有基础队列语义与告警摘要，但还没有完整任务系统：worker pool / 更细粒度 priority policy / 熔断策略 / backlog rebalancing
2. `FINRA` 正式接入
3. `SimFin derived / income / cashflow` 真正进入估值主链路
4. 更完整的行业专属模型：Bank / Insurance / REIT / Biotech / SOTP
5. 更完整的 peer universe 与行业专属 peer rules
6. `history` 按交易日重放估值
7. `US rankings` 还未做到“全量 latest snapshot 覆盖 + 全市场严格估值排序”
8. 平台层 client lifecycle / quota / billing / 控制台还未完成
9. 更完整的 OpenAPI 契约对齐和商业字段
10. `US peers` 当前虽然已有 `snapshot_fallback`，但真实同行集仍不稳定
   - 仍可能退回 `template_only`
   - fallback 已能读取部分真实 `market_cap`
   - `roic` 仍可能为空，取决于 peer 是否已有 `financial_derived_metrics`

### 6.3 中国市场未完成

1. 标准化财报落库主链路
2. 类似美股的 `source_documents -> raw facts -> standardized` 数据库链路
3. 中国专属 `profile / data-quality / financial-quality`
4. 中国市场完整 latest snapshot + schedule 框架
5. 中国专属 peers engine
6. 全市场持久化 rankings

### 6.4 日本市场未完成

1. 正式日本市场数据源接入
2. 正式日本财报标准化与数据库链路
3. 日本专属同行比较与榜单引擎
4. 日本市场产品级 explanation / report
5. 日本市场 latest snapshot + schedule 框架

### 6.5 香港市场未完成

1. 独立 HK controller
2. 正式港股实时/历史行情主源
3. 正式港股财务标准化链路
4. 独立港股估值模型与流动性折价引擎
5. 港股专属 rankings / peers / screener
6. 港股 latest snapshot + schedule 框架

## 7. 当前需要修改或重构的部分

1. 全局 DTO 和 API envelope 已有基础实现，但还未统一切换到单一契约。
2. `history / scenario / screener` 仍有部分通用启发式逻辑，尚未完全市场专属化。
3. `CN / JP / HK peers` 仍是 fallback 逻辑，不是正式同行引擎。
4. `CN rankings` 当前仍是 page-scoped，不是全市场完整排序。
5. 通用快照模型 `StockSnapshot / StockFundamentals` 还缺真实 `market_cap` 等字段，影响非美市场 peers 精度。
6. 美国后台页当前已具备核心运维操作，但还不是正式权限分层、危险操作确认和审计版。
7. 前端页目前是基础可用版，离正式商业产品还有明显差距。
8. 文档体系必须持续收口到统一主文档，避免重复口径再扩散。

## 8. 当前建议研发优先级

### P0

1. 美国正式 peer universe 收口
2. `valuation_jobs` worker pool / priority / alerting 收口
3. `US rankings` 全量 strict ranking engine
4. 平台层 client lifecycle / quota / billing 收口
5. OpenAPI 契约统一收口

### P1

1. watchlist / alert / webhook
2. 历史估值按交易日重放
4. 行业专属模型扩展
5. CN / JP / HK 正式 peers 引擎

### P2

1. 多租户 / 白标
2. 客户后台与用量统计
3. 企业版 API 管理
4. 策略组合页与工作流化产品

## 9. 文档保留决策

### 9.1 核心保留

1. [全球股票估值系统_统一产品文档.md](F:/fairvalue/全球股票估值系统_统一产品文档.md)
2. [美国股票估值.md](F:/fairvalue/美国股票估值.md)
3. [美股估值系统_完整终版方案_v2.docx](F:/fairvalue/美股估值系统_完整终版方案_v2.docx)
4. [中国股票估值引擎_PRD_正式版.docx](F:/fairvalue/中国股票估值引擎_PRD_正式版.docx)
5. [us_equity_valuation_complete_final_plan.md](F:/fairvalue/us_equity_valuation_complete_final_plan.md)
6. [us_equity_valuation_api_openapi_spec.md](F:/fairvalue/us_equity_valuation_api_openapi_spec.md)

### 9.2 不再作为主文档使用

1. 旧的全球 PRD
2. 重复功能清单
3. 旧商业化草稿

这些内容如仍需参考，应只作为历史材料，不再作为执行基线。

## 10. 当前结论

一句话总结当前项目状态：

**美国股票估值主链路已经进入“可定时刷新、可落库、可读库、可继续产品化”的阶段；中国市场已有专属规则和 API 雏形；日本市场已有接口和基础估值能力；香港市场仍主要处于通用层覆盖；整个全球产品距离正式商业化平台还差统一 API、权限计费、全市场持久化和非美正式数据源。**
