# 美国股票估值系统 PRD（Agent 执行版）

更新日期：2026-03-31  
适用对象：产品、后端、数据工程、Codex、Claude Code  
适用范围：美国股票估值系统（US Equities）

终版对齐说明：

1. 本文件已按 [美股估值系统_完整终版方案_v2.docx](F:/fairvalue/美股估值系统_完整终版方案_v2.docx) 重新梳理。
2. 产品规则、数据源层级、方法体系、风险矩阵和输出结构以终版方案 v2 为准。
3. 工程落地层面，当前仓库继续沿用 `Java + Spring Boot + PostgreSQL`，不因为终版方案里出现 `Python + FastAPI` 建议就推翻现有代码基线。
4. Claude Code 与 Codex 的协作应以“对齐终版规则、逐步收敛当前 Java 实现”为目标，而不是重写整套技术栈。

## 1. 文档目的

这份文档不是研究笔记，而是执行型 PRD。目标是让人类开发者、Codex、Claude Code 能基于同一套约束完成以下工作：

1. 搭建美国股票估值的数据层、标准化层、估值层、风险层、解释层和 API 输出层。
2. 明确每个字段应该来自哪里，哪些是主源，哪些只能做补充，哪些绝不能伪造。
3. 约束估值方法、数据质量、风险修正、数据库追溯和前后端输出结构。
4. 为后续代码实现、数据库建模、接口开发、任务拆分和验收提供统一标准。

一句话定义：

**这是一个面向美国股票的、可追溯、可解释、可 API 化的 fair value engine，而不是一个只返回单点目标价的小工具。**

## 2. 产品目标

### 2.1 核心目标

系统必须输出以下结果：

1. `fair_value_low / fair_value_mid / fair_value_high`
2. `blended_intrinsic_value`
3. `margin_of_safety`
4. `verdict`，即低估、合理、轻度高估、高估
5. `scenario_matrix`，至少包含 bear / base / bull
6. `risk_matrix`
7. `implied_expectation`，即 Reverse DCF 反推出来的市场隐含预期
8. `explanation_blocks`，用于前端和 API 展示
9. `data_quality_audit`，明确数据完整性、时效性和可信度

### 2.2 非目标

本期不做以下内容：

1. 自动下单或交易执行
2. 高频或超短线行情策略
3. 纯新闻聚合产品
4. 纯主观 AI 拍脑袋估值
5. 没有来源追溯的自动报告

## 3. 设计原则

### 3.1 强约束

所有 Agent 必须遵守以下规则：

1. 估值至少使用 3 种方法，目标 4 到 5 种方法交叉验证。
2. Reverse DCF 必须始终运行，不能省略。
3. 所有模型结果都必须保留 `assumptions_json`。
4. 所有标准化财务数据都必须能追溯到 `source_document_id`。
5. 所有输出都必须显式携带 `data_version` 或可追溯来源信息。
6. 生产模式禁止用 synthetic 数据伪装成真实估值结果。
7. 当关键财报、股本、债务、SBC、指引缺失时，必须下调 `confidence_level`。
8. 解释层不能写死在业务逻辑里，必须走规则或模板。
9. 行业模板、WACC、风险修正、方法权重必须配置化，不能硬编码在多个模块里。

### 3.2 产品级判断标准

系统更关心“估值流程是否可信”，而不只是“算出一个数字”。

这意味着数据库设计、API 设计、任务拆分都必须围绕以下链路组织：

**原始事实 -> 标准化财务 -> 市场价格 -> 多模型估值 -> 风险修正 -> 解释输出**

## 4. 数据源策略

### 4.1 终版生产源

按终版方案 v2，生产版推荐固定为以下组合：

1. `SEC EDGAR`
2. `公司 IR`
3. `长桥 API`
4. `FRED`
5. `Damodaran`

其中：

1. `SEC EDGAR` 是财报真相层主源。
2. `公司 IR` 是指引、Investor Day、segment KPI、管理层解释的补充主源。
3. `长桥 API` 是正式产品价格层、快照层和 vendor multiple 主源。
4. `FRED` 是 `risk-free rate` 与宏观情景输入主源。
5. `Damodaran` 是 `ERP / 行业 beta / 行业倍数 / country risk premium` 的参数主源。

### 4.2 研究与增强源

以下来源可用于研究、交叉校验或风险增强，但不能替代真相层：

1. `FINRA`
2. `SimFin`
3. `yfinance / Stooq`
4. `FactSet / Bloomberg / Visible Alpha / Refinitiv`

终版定位如下：

| Tier | 数据源 | 定位 | 是否可作为主证据 |
| --- | --- | --- | --- |
| Tier 1 | SEC EDGAR | 财报、股本、治理、披露主源 | 是 |
| Tier 1 | 公司 IR | 指引、Investor Day、segment KPI、管理层说明主源 | 是 |
| Tier 1 | 长桥 API | 正式市场价格、快照、K 线、vendor multiples 主源 | 是 |
| Tier 1.5 | FRED | WACC 的 risk-free rate 与宏观序列主源 | 是 |
| Tier 1.5 | Damodaran | ERP、行业 beta、行业倍数参数主源 | 是 |
| Tier 2 | FINRA | short interest / float 风险增强 | 否，仅进入 risk matrix |
| Tier 2 | SimFin | 标准化财务加速与交叉回查 | 否，不能覆盖 SEC 真相层 |
| Tier 3 | yfinance / Stooq | 研究价量与备份价格 | 仅研究/内部调试 |
| Tier 4 | 社区、论坛、媒体二次转载 | 线索层 | 绝不能做估值主证据 |

### 4.3 字段归属规则

| 字段类别 | 主源 |
| --- | --- |
| ticker / CIK / 证券映射 | SEC EDGAR + 主数据表 |
| 10-K / 10-Q / 8-K / DEF 14A / Form 4 | SEC EDGAR |
| revenue / EBITDA / CFO / capex / FCF / debt / equity / diluted shares / SBC | SEC EDGAR |
| guidance / management commentary / investor day assumptions / segment KPI | 公司 IR |
| current price / OHLCV / market snapshot / PE / PB / market cap / vendor shares | 长桥 API |
| risk-free rate / treasury yield / macro series | FRED |
| ERP / 行业 beta / 行业默认倍数 / country risk premium | Damodaran |
| short interest / float 风险补充 | FINRA |
| 标准化三表加速与回查 | SimFin |
| consensus / street estimates | 第二阶段专业数据源 |

### 4.4 回退规则

1. 长桥短时不可用时，允许使用最近一次成功抓取的市场快照，但必须标记 `stale_market_data=true`。
2. FRED 或 Damodaran 暂时不可用时，允许读取最近一次落库参数，但必须标记 `parameter_snapshot_stale=true`。
3. SEC 披露未更新时，可以继续使用最近一个已确认财报周期，但必须在 `data_quality_audit` 中标记缺口。
4. 公司 IR 缺失时，允许回退为 `guidance_status=filings_only`，但不能假装已有指引。
5. 生产模式禁止因为缺数而自动生成 synthetic 财务数据。
6. 开发模式如确实需要 synthetic 数据，必须在输出中显式标记 `source_mode=synthetic_dev_only`。

## 5. 六层架构

### 5.1 数据源层

负责抓取、缓存、版本管理和原始落库。

必须支持：

1. SEC submissions
2. SEC companyfacts / XBRL facts
3. 公司 IR 文档和结构化摘要
4. 长桥市场快照
5. 长桥日线和盘中关键时点数据

### 5.2 标准化层

负责把不同来源的数据统一成同一套估值输入。

必须完成：

1. 财报字段标准化
2. share count 统一到 diluted shares 口径
3. SBC、lease、pension、convertible、minority interest 调整
4. FY / Q / TTM 生成
5. owner earnings、ROIC、FCF conversion 等派生指标生成

### 5.3 估值引擎层

负责根据公司类型和行业模板选方法、算估值、汇总 blended value。

### 5.4 风险引擎层

负责风险矩阵、WACC 调整、情景权重调整、value trap 标记。

### 5.5 解释层

负责把数据和模型结果转换成用户能读懂的结构化解释块。

### 5.6 输出层

负责网站、API、批量筛选和后台审核共用的统一输出结构。

## 6. 数据库设计总则

推荐数据库：`PostgreSQL`  
推荐缓存：`Redis`  
推荐对象存储：用于 SEC 原文、IR PDF、HTML、解析中间文件

### 6.1 设计原则

1. 主库以结构化数据和追溯为中心，不围绕某一个接口返回临时设计。
2. 原始数据层必须存在，不能只保留处理后的结果。
3. 标准化层必须和原始文档通过 `source_document_id` 相连。
4. 估值结果必须拆成运行总表、方法结果表、情景表、风险表、解释表，而不是全部塞进一个 JSON。
5. JSON 字段可以用来存弹性结构，但不能取代核心关系字段。

## 7. 核心表设计

### 7.1 主数据层

#### `security_master`

用途：股票主表，统一证券主键。  
关键字段：`id`、`ticker`、`symbol_full`、`company_name`、`exchange`、`currency`、`sector`、`industry`、`subindustry`、`company_type`、`sector_template`、`country`、`is_active`

#### `security_identifier_map`

用途：不同源的代码映射。  
关键字段：`security_id`、`source_name`、`source_symbol`、`cik`、`isin`、`cusip`、`sedol`、`exchange_code`、`is_primary`

#### `source_registry`

用途：定义 source hierarchy。  
关键字段：`source_name`、`source_tier`、`source_type`、`priority_rank`、`is_enabled`

### 7.2 原始数据层

#### `source_documents`

用途：保存 SEC 和 IR 文档元数据。  
关键字段：`security_id`、`source_id`、`document_type`、`document_title`、`filing_date`、`accepted_at`、`period_end_date`、`accession_no`、`document_url`、`local_storage_path`、`checksum`、`parsed_status`、`parser_version`

#### `source_document_facts_raw`

用途：保存 EDGAR XBRL 原始 facts。  
关键字段：`source_document_id`、`security_id`、`taxonomy`、`concept_name`、`unit`、`period_start`、`period_end`、`fiscal_year`、`fiscal_period`、`value_numeric`、`value_text`、`segment_name`、`raw_json`

#### `market_data_raw`

用途：保存长桥原始返回，便于回放和重新解析。  
关键字段：`security_id`、`source_id`、`data_type`、`trade_date`、`trade_ts`、`raw_json`

### 7.3 标准化财务层

#### `financial_standardized`

用途：估值主财务表。  
关键字段：`security_id`、`period_type`、`fiscal_year`、`fiscal_period`、`period_start`、`period_end`、`revenue`、`gross_profit`、`ebitda`、`ebit`、`net_income`、`operating_cash_flow`、`capex`、`free_cash_flow`、`cash`、`short_term_debt`、`long_term_debt`、`total_debt`、`equity`、`total_assets`、`total_liabilities`、`diluted_shares`、`basic_shares`、`sbc`、`lease_liabilities`、`pension_liabilities`、`minority_interest`、`goodwill`、`intangibles`、`tax_rate_effective`、`net_debt`、`source_document_id`、`quality_flag_json`

#### `financial_derived_metrics`

用途：标准化后派生指标。  
关键字段：`security_id`、`period_type`、`fiscal_year`、`fiscal_period`、`gross_margin`、`ebit_margin`、`fcf_margin`、`roe`、`roic`、`roa`、`fcf_conversion`、`accruals_ratio`、`net_debt_to_ebitda`、`interest_coverage`、`working_capital_ratio`、`book_value_per_share`、`eps_diluted`、`owner_earnings_estimate`、`altman_z_score`

### 7.4 市场价格层

#### `market_price_daily`

用途：历史日线，用于 historical multiple、走势图和回测。  
关键字段：`security_id`、`trade_date`、`open`、`high`、`low`、`close`、`volume`、`turnover`、`adj_close`、`dividend_adjustment_factor`、`split_adjustment_factor`、`source_id`

#### `market_snapshot`

用途：当前市场快照。  
关键字段：`security_id`、`snapshot_time`、`last_price`、`prev_close`、`open_price`、`high_price`、`low_price`、`volume`、`turnover`、`market_cap_vendor`、`pe_ttm_vendor`、`pb_vendor`、`dividend_yield_vendor`、`eps_ttm_vendor`、`bps_vendor`、`float_shares_vendor`、`total_shares_vendor`、`source_id`

#### `market_intraday_snapshot`

用途：盘中关键时点刷新。  
关键字段：`security_id`、`snapshot_time`、`last_price`、`volume`、`turnover`、`change_pct`、`source_id`

### 7.5 配置层

#### `valuation_method_catalog`

用途：估值方法定义，不把方法写死在代码里。  
关键字段：`method_name`、`method_group`、`is_enabled`、`description`

#### `sector_template_config`

用途：行业模板和默认权重。  
关键字段：`sector_template`、`primary_methods_json`、`primary_metrics_json`、`forbidden_methods_json`、`default_weights_json`、`risk_notes_json`、`safety_margin_rule_json`

#### `valuation_parameter_set`

用途：WACC、terminal growth、ERP 等参数配置。  
关键字段：`security_id`、`sector_template`、`parameter_type`、`parameter_key`、`parameter_value`、`effective_from`、`effective_to`、`source_note`

### 7.6 质量与风险层

#### `data_quality_audit`

用途：数据完整性和时效性审计。  
关键字段：`security_id`、`audit_date`、`latest_10k_date`、`latest_10q_date`、`has_recent_8k`、`share_count_verified`、`sbc_quantified`、`guidance_status`、`net_debt_updated`、`tax_rate_updated`、`litigation_captured`、`convertible_identified`、`data_quality_score`、`confidence_level`、`missing_items_json`、`warning_flags_json`

#### `financial_quality_scores`

用途：财务质量引擎输出。  
关键字段：`security_id`、`period_type`、`period_end`、`earnings_quality_score`、`revenue_quality_score`、`balance_sheet_score`、`capital_efficiency_score`、`capital_allocation_score`、`total_quality_score`、`quality_score_breakdown_json`、`red_flags_json`、`forensic_flags_json`

#### `risk_scores`

用途：风险矩阵结果表。  
关键字段：`security_id`、`valuation_run_id`、`risk_type`、`probability`、`impact`、`score`、`adjustment_type`、`adjustment_value`、`note`

### 7.7 估值运行层

#### `valuation_runs`

用途：一次完整估值的总表。  
关键字段：`security_id`、`valuation_date`、`run_mode`、`current_price`、`fair_value_low`、`fair_value_mid`、`fair_value_high`、`blended_intrinsic_value`、`confidence_level`、`margin_of_safety`、`implied_expectation_label`、`sector_template`、`company_type`、`weighted_value`、`final_verdict`、`buy_zone_low`、`buy_zone_high`、`hold_zone_low`、`hold_zone_high`、`avoid_zone_low`、`avoid_zone_high`、`report_json`

#### `valuation_method_results`

用途：每个估值方法一行。  
关键字段：`valuation_run_id`、`security_id`、`method_name`、`bear_value`、`base_value`、`bull_value`、`weight`、`is_primary_method`、`input_snapshot_date`、`assumptions_json`、`sensitivity_json`、`notes`

#### `scenario_results`

用途：bear / base / bull 情景结果。  
关键字段：`valuation_run_id`、`scenario_name`、`probability_weight`、`price_target`、`revenue_cagr`、`ebitda_margin`、`fcf_margin`、`notes_json`

#### `reverse_dcf_results`

用途：反推市场隐含预期。  
关键字段：`valuation_run_id`、`implied_revenue_cagr`、`implied_ebitda_margin`、`implied_fcf_margin`、`terminal_growth`、`wacc`、`implied_expectation_label`、`notes_json`

### 7.8 解释与输出层

#### `report_blocks`

用途：结构化解释块，供前端和 API 共用。  
关键字段：`valuation_run_id`、`block_type`、`block_content`、`display_order`

### 7.9 任务与调度层

#### `ingestion_jobs`

用途：采集任务状态。  
关键字段：`job_type`、`security_id`、`status`、`started_at`、`finished_at`、`error_message`、`retry_count`、`payload_json`

#### `normalization_jobs`

用途：标准化任务状态。

#### `valuation_jobs`

用途：估值重算任务状态。

## 8. MVP 必做表

第一阶段先落地以下 10 张表：

1. `security_master`
2. `security_identifier_map`
3. `source_documents`
4. `source_document_facts_raw`
5. `financial_standardized`
6. `market_price_daily`
7. `market_snapshot`
8. `data_quality_audit`
9. `valuation_runs`
10. `valuation_method_results`

这 10 张表已经足够跑通单股估值 MVP，包括：

1. 股票识别
2. 标准化财报
3. DCF
4. Historical Multiple
5. Relative Valuation
6. Reverse DCF
7. fair value range
8. 数据质量审计
9. 基础 API 输出

## 9. 估值方法规则

### 9.1 方法最少数量

1. 每个标的至少运行 3 种方法。
2. 正常目标是 4 到 5 种方法交叉验证。
3. `Reverse DCF` 必跑，但默认不直接参与 `blended_intrinsic_value` 加权。
4. 如果某种方法因数据不完整不可用，必须在输出里说明原因和缺失字段。

### 9.2 Always-On 核心方法

终版方案 v2 要求优先固化 4 个 Always-On 方法：

| 方法 | 默认权重 | 说明 |
| --- | --- | --- |
| `DCF / FCFF` | 30% - 40% | 主模型；10 年显式预测 + 终值；对增长与 WACC 最敏感 |
| `Historical Multiple` | 15% - 25% | 看当前估值相对自身历史区间的高低 |
| `Relative Valuation` | 20% - 30% | 行业中位数 + 精选 peer set 交叉验证 |
| `Reverse DCF` | 必跑 | 反推当前价格隐含的增长、利润率和 FCF 假设 |

### 9.3 按公司类型自动选主方法

| 公司类型 | 主方法 |
| --- | --- |
| Compounder / Profitable Growth | DCF + Historical Multiple + Relative Valuation + Reverse DCF |
| Hyper-growth SaaS | EV/NTM Revenue + EV/Gross Profit + Rule of 40 + Reverse DCF |
| Cyclical | Mid-cycle EV/EBITDA + Historical Range + Reverse DCF |
| Bank / Insurance | P/TBV + ROE-COE + DDM |
| REIT | AFFO Multiple + NAV + DDM |
| Biotech Pre-revenue | rNPV + Cash Runway + Comparable M&A |
| Conglomerate / HoldCo | SOTP / NAV + Look-through Valuation |

### 9.4 通用调整规则

1. 必须使用 `diluted_shares`。
2. SBC 必须进入真实成本口径。
3. 必须做 `net cash / net debt` 调整。
4. 周期股不能直接使用 peak-cycle earnings。
5. 金融股、保险、REIT、Biotech 不能简单复用工业股 DCF。
6. 关键数据缺失时，必须降低复杂模型权重，提高 Historical / Relative 权重。
7. dual-class、治理差、持续稀释、弱资本配置应触发折价或更高 `margin_of_safety`。

## 10. 数据质量审计规则

系统必须检查以下项目：

1. 最新 10-K 是否纳入。
2. 最新 10-Q 是否纳入。
3. 最近 90 天是否存在重大 8-K。
4. diluted share count 是否核对。
5. SBC 是否量化。
6. cash / debt / net debt 是否更新。
7. tax rate 是否更新。
8. convertibles / warrants / options 是否识别。
9. guidance 是否捕获。
10. insider / governance 信号是否至少被审计。

数据质量对输出的影响：

1. 缺最新季度财报时，不允许高置信度输出。
2. 缺 diluted shares 时，不允许输出“高可信 fair value”。
3. 缺 guidance 时，允许估值，但要标记 `guidance_status=filings_only`。
4. 关键字段缺失时，应降低复杂模型权重，提高相对估值和历史估值权重。

## 11. 风险引擎规则

### 11.1 风险矩阵维度

必须覆盖以下风险类型：

1. earnings miss / guidance cut
2. multiple compression
3. balance sheet stress
4. competitive disruption
5. regulatory / legal
6. macro / recession
7. management / governance
8. dilution
9. commodity / input cost
10. FX / geopolitics
11. value trap

### 11.2 风险调整方式

风险必须映射到以下调整类型之一：

1. `wacc`
2. `scenario_weight`
3. `margin_of_safety`

### 11.3 默认调整准则

1. 1 个 High/High 风险，可上调 WACC 0.5 到 1.0 个百分点。
2. 多个 High/High 风险，可上调 WACC 1.0 到 2.0 个百分点，并压低 bull 权重。
3. 二元事件更适合进入情景概率，而不是简单堆到 WACC 里。
4. 若公司同时具备低估值、弱治理、差资本配置、弱现金流四类特征，应单独打 `value_trap_flag`。

## 12. API 输出标准

终版输出必须固定分成 3 层：

1. `Summary`
2. `Decision`
3. `Explanation`

其中：

1. `Summary` 负责第一屏结论和筛选器复用。
2. `Decision` 负责真实决策依据，包括 `scenario_matrix / risk_matrix / implied_expectation / data_quality_audit / value_trap_flag`。
3. `Explanation` 负责结构化解释块，供前端、API 和导出报告复用。

### 12.1 Profile

`GET /v1/us-equities/{ticker}/profile`

返回至少包含：

```json
{
  "ticker": "AAPL",
  "company_name": "Apple Inc.",
  "exchange": "NASDAQ",
  "sector": "Technology",
  "industry": "Consumer Electronics",
  "market_cap": 0,
  "diluted_shares": 0,
  "company_type": "compounder",
  "sector_template": "us_tech_compounder"
}
```

### 12.2 Data Quality

`GET /v1/us-equities/{ticker}/data-quality`

必须返回：

1. 最新 10-K / 10-Q 日期
2. 是否存在近期 8-K
3. 是否验证股本
4. 是否量化 SBC
5. guidance 状态
6. confidence level
7. missing_items
8. warning_flags

### 12.3 Financial Quality

`GET /v1/us-equities/{ticker}/financial-quality`

必须返回：

1. earnings_quality_score
2. revenue_quality_score
3. balance_sheet_score
4. capital_efficiency_score
5. capital_allocation_score
6. total_quality_score
7. red_flags
8. forensic_flags
9. altman_z
10. owner_earnings

### 12.4 Run Valuation

`POST /v1/us-equities/{ticker}/valuation/run`

请求体：

```json
{
  "style": "balanced",
  "horizon": "6-18m",
  "use_consensus": true,
  "force_methods": [],
  "custom_assumptions": {
    "wacc": null,
    "terminal_growth": null,
    "commodity_deck": null
  }
}
```

响应体必须包含：

1. `valuation_methods`
2. `method_outputs`
3. `scenario_matrix`
4. `blended_intrinsic_value`
5. `fair_value_range`
6. `confidence_level`
7. `margin_of_safety`
8. `implied_expectation`
9. `risk_matrix`
10. `explanation_blocks`
11. `data_quality_audit`

### 12.5 Summary

`GET /v1/us-equities/{ticker}/valuation/summary`

必须返回：

1. `verdict`
2. `fair_value_range`
3. `upside_downside`
4. `confidence_level`
5. `buy_zone`
6. `hold_zone`
7. `avoid_zone`

### 12.6 Report

`GET /v1/us-equities/{ticker}/valuation/report`

必须返回标准化报告块，而不是拼接的大段不可解析文本。

Explanation block 最终至少包含：

1. `one_line_verdict`
2. `executive_summary`
3. `data_quality_audit`
4. `business_and_moat`
5. `financial_quality_scorecard`
6. `growth_and_catalysts`
7. `risk_matrix`
8. `valuation_breakdown`
9. `scenario_matrix`
10. `final_fair_value`
11. `actionable_framework`
12. `uncertainty_and_error_sources`

## 13. Agent 开发规则

这部分是给 Codex / Claude Code 的明确约束。

### 13.1 允许做的事

1. 新增数据库表、DTO、service、controller、配置类、定时任务。
2. 把规则提取到配置表或配置文件。
3. 为估值方法增加 `assumptions_json`、`sensitivity_json`、`source_document_id` 追溯链路。
4. 为 API 增加 `data_quality`、`risk_matrix`、`report_blocks` 等结构化输出。

### 13.2 禁止做的事

1. 禁止把 synthetic 数据伪装成真实生产输出。
2. 禁止因为缺数就硬造高置信度 fair value。
3. 禁止把 explanation 文案散落硬编码在多个 service 里。
4. 禁止把行业模板、WACC、方法权重写死成不可配置常量。
5. 禁止删掉来源追溯字段。

### 13.3 代码边界建议

终版方案原文给出的理想目录是：

1. `ingestion/`
2. `normalization/`
3. `classification/`
4. `quality_engine/`
5. `valuation_engine/`
6. `risk_engine/`
7. `report_engine/`
8. `api/`
9. `repository/`
10. `config/`

但当前仓库为 `Java + Spring Boot`，Agent 不应为了“目录名字更像终版方案”而推翻现有结构。当前应按职责对齐，而不是技术栈重写。建议映射如下：

| 终版层 | 当前 Java 目录/模块 |
| --- | --- |
| ingestion | `com.fairvalue.engine.us.*SyncService`, `UsSecClient`, `UsCompanyIrClient`, `UsStooqClient` |
| normalization | `UsFinancialStandardizationService`, `UsFinancialDerivedMetricsService` |
| classification | `UsSecurityClassificationService`, `rules/*.json` |
| quality_engine | `UsDataQualityAuditService`, `UsFinancialQualityScoringService` |
| valuation_engine | `UsConfiguredValuationModelsService`, `UsValuationConfigService`, `valuation/strategy/*` |
| risk_engine | `UsRiskMatrixService` |
| report_engine | `UsValuationPersistenceService`, `report_blocks`, summary/report DTO |
| api | `com.fairvalue.engine.api.*` |
| repository | `com.fairvalue.engine.repository.*` |
| config | `com.fairvalue.engine.config.*` |

### 13.4 测试要求

至少覆盖以下内容：

1. SEC 文档解析正确性
2. 标准化财报口径一致性
3. Reverse DCF 必跑校验
4. 至少 3 种估值方法的选择器测试
5. 风险矩阵对 WACC 和情景权重的影响测试
6. 当关键数据缺失时，`confidence_level` 降级测试
7. API 返回结构稳定性测试

## 14. 推荐实施顺序

### 阶段 0：基础设施

1. 建主数据表和原始层表
2. 建标准化财务表和市场快照表
3. 接入 PostgreSQL 和对象存储
4. 建基础任务表和审计日志

### 阶段 1：数据抓取与标准化

1. 接 SEC submissions
2. 接 SEC companyfacts
3. 建 XBRL facts 落库和标准化流程
4. 接公司 IR 文档抓取与元数据落库
5. 接长桥快照和日线

### 阶段 2：估值 MVP

1. 股票识别与行业模板选择
2. data quality audit
3. financial quality scores
4. Relative Valuation
5. Historical Multiple
6. DCF
7. Reverse DCF

### 阶段 3：风险与报告

1. risk matrix
2. scenario engine
3. margin of safety
4. explanation blocks
5. summary/report API

### 阶段 4：扩展能力

1. consensus 数据源
2. 更多行业模板
3. 批量筛选
4. watchlist 和告警
5. 全市场 universe 管理

## 15. 当前实现与目标差距

本节用于指导后续 Agent 优先补哪些短板。

### 15.1 当前优先要补的能力

1. 用长桥替换或补强当前 `Stooq + SEC enrich` 价格源，形成真正的市场快照主源。
2. 补上 `FRED + Damodaran`，让 `risk-free rate / ERP / 行业 beta / 行业倍数` 不再停留在静态配置或启发式逻辑。
3. 把 `Relative Valuation` 从模板目标倍数升级成真正的 `peer set` 引擎。
4. 把 `Historical Multiple` 从稀疏样本版升级成正式历史区间引擎。
5. 把 `AAPL` 这类 case 的 `financial_standardized / financial_derived_metrics` 继续补厚，减少 profile fallback。
6. 把 Explanation blocks 完整对齐到终版 12 个固定块。
7. 把调度链路补成正式 `ingestion / normalization / valuation jobs`。

### 15.2 可以临时接受的过渡方案

1. 在开发阶段，可以先用 `SEC + Stooq` 做单股估值联调，但必须明确它只是研究/过渡价格层。
2. 在没有 consensus 的阶段，可以返回 `use_consensus=false` 或 `guidance_status=filings_only`。
3. 在没有 IR 结构化摘要前，可以先落 IR 文档元数据和原文链接。
4. 在没有 FRED / Damodaran 在线同步前，可以先用落库参数快照，但必须标记参数版本。

### 15.3 不可接受的过渡方案

1. 用合成财务数据给真实用户输出 fair value。
2. 用单一相对估值法替代多模型框架。
3. 缺 Reverse DCF。
4. 缺 `data_quality_audit`。
5. API 不返回风险矩阵和解释块。

## 16. 验收标准

### 16.1 功能验收

1. 输入一个美股 ticker，系统能生成完整估值报告。
2. 报告必须有 fair value range，不能只有单点估值。
3. 报告必须有 risk matrix。
4. 报告必须有 Reverse DCF。
5. 报告必须有 confidence level。
6. 至少 3 种估值方法参与计算。
7. API 输出结构稳定，可重复调用。

### 16.2 质量验收

1. 缺关键财报或股本数据时，不允许返回高置信度。
2. 周期股不得直接使用 peak multiple。
3. 银行不得直接套工业股结论。
4. pre-revenue biotech 不得只给传统 P/E。
5. SBC、稀释股本、净债务必须进入主计算。
6. 估值结果必须能追溯到输入来源。

## 17. 给 Agent 的一句话任务定义

**按终版方案 v2，把当前 Java 代码持续收敛到“SEC/IR/长桥/FRED/Damodaran -> 标准化财务 -> 多模型估值 -> 风险矩阵 -> Summary/Decision/Explanation”这条主链路；不要重写技术栈，不要先拼页面，不要把研究版能力当成产品版能力。**
